# ============================================
# ENVIRONMENT VARIABLES LIFECYCLE GUIDE
# ============================================
# This file is organized by when you obtain each variable:
#   STAGE 1: Prerequisites - Set these before any deployment
#   STAGE 2: Deployment Outputs - Obtained from 'make deploy'
#   STAGE 3: Integration Outputs - Obtained from integration steps
#
# Copy this file to .env and fill in values as you progress through deployment


# ============================================
# STAGE 1: PREREQUISITES
# ============================================
# Required before running any make targets

# ----- Core Google Cloud Configuration -----
# Your Google Cloud Project ID
PROJECT_ID=your-project-id

# Your Google Cloud Project Number (find in Console > Project Info)
PROJECT_NUMBER=123456789012

# Deployment region (must support Vertex AI Agent Engine)
# Options: us-central1, europe-west4, asia-northeast1
LOCATION=us-central1

# GCS bucket name for staging agent artifacts (you must include the gs:// prefix)
# Will be created if it doesn't exist
STAGING_BUCKET=your-staging-bucket

# Google API Key for Gemini
# Generate at: https://aistudio.google.com/
GOOGLE_API_KEY=your-google-api-key

# ----- Security Tool Integrations (Optional) -----
# Configure based on which tools you want to use

# Chronicle SIEM Configuration
# Required if using Chronicle SecOps MCP tools
CHRONICLE_CUSTOMER_ID=01234567-abcd-4321-1234-0123456789ab
# Optional: defaults to PROJECT_ID
CHRONICLE_PROJECT_ID=your-chronicle-project-id
CHRONICLE_REGION=us
# Path to service account JSON file for SecOps authentication
SECOPS_SA_PATH=/path/to/service-account.json

# SOAR Platform Configuration
# Required if using SOAR MCP tools
SOAR_URL=https://YOURS.siemplify-soar.com:443
SOAR_APP_KEY=your-soar-api-key

# VirusTotal API Configuration
# Required if using Google Threat Intelligence MCP tools
# Get your API key at: https://www.virustotal.com/gui/my-apikey
VT_APIKEY=your-virustotal-api-key

# ----- OAuth Configuration (Optional) -----
# Required only if using OAuth-based authentication
OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
OAUTH_CLIENT_SECRET=your-client-secret
OAUTH_AUTH_URI=https://accounts.google.com/o/oauth2/auth
OAUTH_TOKEN_URI=https://oauth2.googleapis.com/token

# ----- AgentSpace Pre-configuration (Optional) -----
# These can be set before deployment if you have existing resources

# AgentSpace project configuration (defaults to main project if not specified)
AGENTSPACE_PROJECT_ID=  # Optional: defaults to PROJECT_ID
AGENTSPACE_PROJECT_NUMBER=  # Optional: defaults to PROJECT_NUMBER

# AgentSpace app ID (create in Discovery Engine console or via API)
# Format: <app-name>_<timestamp> (e.g., agentic-soc-agentspace-app_1757549640237)
AGENTSPACE_APP_ID=

# AgentSpace datastore ID for AI runbooks
# Format: <datastore-name>_<timestamp> (e.g., agentic-soc-ai-runbooks-datastore_1757549407776)
AGENTSPACE_DATA_STORE_ID=

# Vertex AI Datastore path (if using existing datastore)
# Format: projects/<PROJECT_NUMBER>/locations/global/collections/default_collection/dataStores/<DATASTORE_ID>
DATASTORE_PATH=

# Optional: Collection and assistant names (defaults shown)
AGENTSPACE_COLLECTION=default_collection  # Optional: defaults to 'default_collection'
AGENTSPACE_ASSISTANT=default_assistant    # Optional: defaults to 'default_assistant'

# Agent display configuration
AGENT_DISPLAY_NAME=Security Analyst Agent
AGENT_DESCRIPTION=AI-powered security analyst with MCP tools
AGENT_TOOL_DESCRIPTION=Analyzes security events and responds to incidents


# ============================================
# STAGE 2: DEPLOYMENT OUTPUTS
# ============================================
# These values are obtained after running 'make deploy'
# The deployment will display these values - save them here

# Reasoning Engine ID (just the numeric ID)
# Obtained from deployment output
# Example: 8293875693058523136
REASONING_ENGINE=

# Agent Engine resource name (full path)
# Obtained from deployment output
# Format: projects/<PROJECT_NUMBER>/locations/<LOCATION>/reasoningEngines/<ENGINE_ID>
AGENT_ENGINE_RESOURCE_NAME=


# ============================================
# STAGE 3: INTEGRATION OUTPUTS
# ============================================
# These values are obtained during integration steps

# OAuth Authorization ID
# Obtained after running 'make oauth-create'
# Example: my-oauth-auth
OAUTH_AUTH_ID=

# AgentSpace Agent ID
# Obtained after running 'make agentspace-register' or 'make agentspace-link-agent'
# Example: 9239948638551901607
AGENTSPACE_AGENT_ID=


# ============================================
# NOTES
# ============================================
# Deployment Workflow:
# 1. Copy this file to .env
# 2. Fill in STAGE 1 variables
# 3. Run 'make deploy' and save STAGE 2 outputs
# 4. Run integration commands and save STAGE 3 outputs
#
# Important:
# - Do not commit .env with real values to version control
# - Ensure proper IAM permissions for your service account
# - Enable required APIs in your Google Cloud project
# - See docs/DEPLOYMENT_WORKFLOW.md for detailed instructions
